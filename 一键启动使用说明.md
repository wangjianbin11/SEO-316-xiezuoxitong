# 一键启动脚本使用说明

## 概述

提供两个智能一键启动脚本，自动检测并安装所有依赖：

- **Windows**: `一键启动-Windows.bat`
- **Mac**: `一键启动-Mac.command`

## 功能特性

### 自动检测和安装

| 组件 | Windows | Mac |
|------|---------|-----|
| Python | ✅ 检测版本 | ✅ 检测版本 |
| pip | ✅ 自动升级 | ✅ 自动升级 |
| 虚拟环境 | ✅ 自动创建 | ✅ 自动创建 |
| 项目依赖 | ✅ 自动安装 | ✅ 自动安装 |
| Playwright | ✅ 自动安装 | ✅ 自动安装 |
| Chromium | ✅ 自动下载 | ✅ 自动下载 |
| GUI 启动 | ✅ 自动启动 | ✅ 自动启动 |

### 错误处理

- Python 未安装 → 提供安装指引
- 依赖安装失败 → 显示详细错误信息
- 网络问题 → 提供镜像解决方案
- 安装失败 → 暂停显示错误，不直接退出

## Windows 使用方法

### 方式1：双击运行
1. 双击 `一键启动-Windows.bat`
2. 等待自动安装完成
3. GUI 自动启动

### 方式2：右键运行（首次）
1. 右键点击 `一键启动-Windows.bat`
2. 选择"以管理员身份运行"
3. 等待自动安装完成
4. GUI 自动启动

### 首次运行流程

```
[1/6] 检测 Python 环境...
  √ Python 已安装: Python 3.14.0

[2/6] 检查 pip 是否需要升级...
  √ pip 已准备就绪

[3/6] 安装 Python 依赖包...
  正在安装依赖，请稍候...
  √ Python 依赖安装完成

[4/6] 检查 Playwright 安装...
  → Playwright 未安装，正在安装...
  √ Playwright 安装完成

[5/6] 检查 Chromium 浏览器...
  → Chromium 未安装，正在安装...
  这将下载约 150-200MB 的文件，请耐心等待...
  √ Chromium 安装完成

[6/6] 启动图形界面...
  正在启动 SEO 内容生成器...
```

## Mac 使用方法

### 方式1：双击运行
1. 双击 `一键启动-Mac.command`
2. 如果提示"无法打开"，右键点击 → "打开"
3. 等待自动安装完成
4. GUI 自动启动

### 方式2：终端运行
```bash
chmod +x 一键启动-Mac.command
./一键启动-Mac.command
```

### 首次运行流程

```
[1/6] 检测 Python 环境...
  √ Python 已安装: Python 3.x.x

[2/6] 检测 pip...
  √ pip 已准备就绪

[3/6] 检查虚拟环境...
  → 创建虚拟环境...
  √ 虚拟环境创建完成

[4/6] 安装 Python 依赖包...
  → 正在安装依赖，请稍候...
  √ Python 依赖安装完成

[5/6] 检查 Playwright...
  → Playwright 未安装，正在安装...
  √ Playwright 安装完成
  → 检查 Chromium 浏览器...
  → Chromium 未安装，正在安装...
  这将下载约 150-200MB 的文件，请耐心等待...
  √ Chromium 安装完成

[6/6] 启动图形界面...
  正在启动 SEO 内容生成器...
```

## 常见问题

### Q: Windows 提示"未找到 Python"？
**A**: 请先安装 Python：
1. 访问 https://python.org/downloads/
2. 下载 Python 3.8 或更高版本
3. 安装时务必勾选 **"Add Python to PATH"**
4. 重新运行一键启动脚本

### Q: Mac 提示"未找到 Python 3"？
**A**: 请先安装 Python：
```bash
# 使用 Homebrew 安装（推荐）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install python3
```

### Q: Playwright 或 Chromium 下载失败？
**A**: 网络问题，尝试使用镜像：

**Windows (PowerShell)**:
```powershell
$env:PLAYWRIGHT_DOWNLOAD_HOST="https://npmmirror.com/mirrors/playwright/"
```

**Mac/Linux**:
```bash
export PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright/
playwright install chromium
```

### Q: 虚拟环境占用空间太大？
**A**: 可以删除虚拟环境重新创建：
```bash
# 删除虚拟环境
rm -rf venv

# 重新运行一键启动（会自动重建）
./一键启动-Mac.command
```

### Q: 如何跳过某些检测步骤？
**A**: 编辑脚本，注释掉不需要的步骤。但不建议新手操作。

### Q: Playwright 安装很慢？
**A**: Chromium 约 150-200MB：
- 首次安装需要下载
- 后续启动会跳过此步骤
- 建议在网络良好时安装

### Q: Windows 杀毒软件报毒？
**A**: 添加信任：
1. Windows Defender → 病毒和威胁防护 → 管理排除项
2. 添加项目文件夹到排除列表

## 手动安装（如自动安装失败）

### Python
- Windows: https://python.org/downloads/
- Mac: `brew install python3`

### Playwright & Chromium
```bash
pip install playwright
playwright install chromium
```

### 项目依赖
```bash
pip install -e .
```

## 系统要求

### Windows
- Windows 10/11
- Python 3.8+
- 网络连接

### Mac
- macOS 10.15+
- Python 3.8+
- 网络连接
- Xcode Command Line Tools（部分情况需要）

## 依赖大小

| 组件 | 大小 |
|------|------|
| Python | ~25-30 MB |
| 虚拟环境 | ~50-100 MB |
| 项目依赖 | ~100-200 MB |
| Playwright | ~50 MB |
| Chromium | ~150-200 MB |
| **总计** | **~400-600 MB** |

## 技术细节

### Windows 批处理脚本
- 使用 `chcp 65001` 设置 UTF-8 编码
- 使用 `%~dp0` 获取脚本所在目录
- 使用 `python -c` 检测模块是否安装
- 使用 `2>nul` 抑制错误输出

### Mac Shell 脚本
- 使用 `#!/bin/bash` shebang
- 使用 `cd "$(dirname "$0")"` 获取脚本所在目录
- 使用 `python3 -c` 检测模块
- 使用 `source venv/bin/activate` 激活虚拟环境

## 更新日志

### v1.0 (2025-02-08)
- ✅ 初始版本
- ✅ Windows 一键启动脚本
- ✅ Mac 一键启动脚本
- ✅ 自动检测和安装所有依赖
- ✅ 智能错误处理
- ✅ 进度显示
