# SEO Content Generator - 新电脑使用指南

> 快速开始 | 3分钟上手

---

## 🚀 最快上手方式

### 你需要准备：

1. **这个项目文件夹** (`seo-content-generator`)
2. **Python 3.11+** （如果没有，去 https://www.python.org/downloads/ 下载）
3. **三个 API 密钥**：
   - OpenRouter: https://openrouter.ai/ （约 $5 起步）
   - Google Search: https://console.cloud.google.com/ （每天 100 次免费）
   - WordPress 应用密码（在你的网站后台生成）

---

## 📦 第一步：复制项目

### 从旧电脑复制：

**需要复制的文件夹：**
```
seo-content-generator/
├── src/              # 代码
├── config/           # 配置（重要！）
├── pyproject.toml    # 依赖
├── .env              # 密钥（重要！）
├── 快速启动.bat      # Windows
└── 快速启动.sh       # Mac
```

**复制方法：**
- U 盘复制
- 云盘（百度网盘、阿里云盘等）
- 压缩成 .zip 后发送

---

## ⚙️ 第二步：在新电脑上安装

### Windows 用户：

1. **安装 Python**
   - 访问 https://www.python.org/downloads/
   - 下载 Python 3.11 或 3.12
   - 安装时 **务必勾选 "Add Python to PATH"**
   - 重启电脑

2. **安装项目**
   - 将项目文件夹复制到电脑（例如桌面）
   - 双击 `快速启动.bat`
   - 按提示操作

### Mac 用户：

1. **安装 Python**
   - 打开终端（在 Launchpad 搜索"终端"）
   - 运行：`brew install python@3.11`
   - 或访问 https://www.python.org/downloads/ 下载安装

2. **安装项目**
   - 打开终端
   - 输入：`cd ~/seo-content-generator`
   - 运行：`bash 快速启动.sh`
   - 按提示操作

---

## 🔑 第三步：配置密钥

首次运行启动脚本时，会提示你创建 `.env` 文件。

**打开 `.env` 文件，填写：**

```env
# OpenRouter API（文章生成）
OPENAI_API_KEY=sk-or-v1-你的密钥

# Google Search API（搜索分析）
GOOGLE_SEARCH_API_KEY=AIzaSy你的密钥
GOOGLE_SEARCH_ENGINE_ID=你的ID

# WordPress（自动发布）
WORDPRESS_SITE_URL=https://你的网站.com/
WORDPRESS_USERNAME=你的用户名
WORDPRESS_APP_PASSWORD=abcd 1234 efgh 5678
```

**如何获取这些密钥：**

| 服务 | 获取地址 | 费用 |
|------|----------|------|
| OpenRouter | https://openrouter.ai/ | 按使用量计费 |
| Google Search | https://console.cloud.google.com/ | 每天 100 次免费 |
| WordPress | 网站后台 → 用户 → 应用密码 | 免费 |

---

## ✅ 第四步：开始使用

### 打开命令行工具

**Windows:**
- 按 `Win + R`
- 输入 `cmd` 回车
- 进入项目目录：`cd 桌面\seo-content-generator`

**Mac:**
- 打开终端
- 进入项目目录：`cd ~/seo-content-generator`

### 运行命令

```bash
# 生成文章（推荐）
seo-gen generate-advanced "关键词"

# 例如：
seo-gen generate-advanced "dropshipping automation"
seo-gen generate-advanced "shopify optimization"
```

**其他选项：**
```bash
# 只生成文章，不发布到网站
seo-gen generate-advanced "关键词" --skip-wordpress

# 不生成图片
seo-gen generate-advanced "关键词" --skip-images

# 查看帮助
seo-gen --help
```

---

## ❓ 不需要 Claude Code！

**这个项目是完全独立的 Python 程序，不需要 Claude Code！**

你只需要：
1. Python 环境
2. .env 配置文件
3. API 密钥

**Claude Code 只是一个开发工具，不是必需的。**

---

## 📞 遇到问题？

### 常见错误

**"不是内部或外部命令"**
→ Python 没安装或没添加到 PATH，重新安装 Python

**"No module named 'xxx'"**
→ 依赖没装好，运行：`pip install -e .`

**"401 Unauthorized"**
→ WordPress 密码错误，检查 `.env` 中的配置

**"API key invalid"**
→ API 密钥错误，检查 `.env` 中的密钥是否正确复制

### 完整文档

查看 `DEPLOYMENT_GUIDE.md` 获取详细指南

---

## 📋 快速命令参考

| 功能 | 命令 |
|------|------|
| 生成文章 | `seo-gen generate-advanced "关键词"` |
| 只生成，不发布 | `seo-gen generate-advanced "关键词" --skip-wordpress` |
| 不生成图片 | `seo-gen generate-advanced "关键词" --skip-images` |
| 查看帮助 | `seo-gen --help` |

---

## 🎯 示例

```bash
# 生成关于 "dropshipping tips" 的完整文章
seo-gen generate-advanced "dropshipping tips"

# 生成关于 "shopify automation" 的文章，但不发布
seo-gen generate-advanced "shopify automation" --skip-wordpress
```

---

**开始创作吧！🎉**
