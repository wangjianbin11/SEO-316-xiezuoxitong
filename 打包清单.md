# SEO Content Generator - 打包清单

> 将此项目迁移到新电脑需要复制的文件清单

---

## 必需文件（必须复制）

### 核心代码
```
src/                          # 源代码目录
├── seo_gen/                 # 主包
│   ├── __init__.py
│   ├── main.py              # 主程序入口
│   ├── config.py            # 配置模块
│   └── modules/             # 所有功能模块
│       ├── content.py       # 内容生成
│       ├── image.py         # 图片生成
│       ├── knowledge.py     # 知识库管理
│       ├── llm.py           # LLM 客户端
│       ├── quality.py       # 质量检测
│       ├── serp.py          # SERP 分析
│       ├── title.py         # 标题生成
│       ├── structure.py     # 结构分析
│       ├── outline.py       # 大纲生成
│       ├── workflow.py      # 工作流编排
│       ├── wordpress.py     # WordPress 发布
│       ├── feishu.py        # 飞书集成
│       └── __init__.py
└── seo_content_generator.egg-info/  # 包信息（可选）
```

### 配置文件
```
config/                       # 配置目录
└── knowledge/               # 知识库（重要！）
    ├── geo_strategy.md      # GEO 策略
    ├── janson_jieshao.txt   # 站长介绍
    ├── kehuhuaxiang.txt     # 客户画像
    ├── qiyejieshao.txt      # 企业介绍
    ├── xiezuojianyi.txt     # 协作建议
    └── yewuliucheng.txt     # 业务流程

pyproject.toml               # 依赖配置
.env                         # 环境变量（包含密钥！）
.env.template               # 环境变量模板
```

### 启动脚本
```
快速启动.bat                  # Windows 快速启动脚本
快速启动.sh                  # Mac/Linux 快速启动脚本
DEPLOYMENT_GUIDE.md          # 部署指南
```

---

## 可选文件（可以不复制）

```
outputs/                     # 输出目录（之前的生成结果）
README.md                    # 项目说明
*.md                         # 其他 Markdown 文件
.egg-info/                   # 安装生成的文件
__pycache__/                 # Python 缓存
*.pyc                        # 编译的 Python 文件
```

---

## 复制方法

### 方法 1: 手动复制（推荐）

1. 将整个 `seo-content-generator` 文件夹复制到 U 盘或云盘
2. 在新电脑上解压到合适的位置
3. 按照 `DEPLOYMENT_GUIDE.md` 中的步骤操作

### 方法 2: 压缩包

**Windows:**
1. 右键点击 `seo-content-generator` 文件夹
2. 选择"发送到" → "压缩(zipped)文件夹"
3. 复制生成的 .zip 文件

**Mac:**
1. 右键点击 `seo-content-generator` 文件夹
2. 选择"压缩"
3. 复制生成的 .zip 文件

---

## 复制后在新电脑上的首次操作

### Windows
1. 双击 `快速启动.bat`
2. 脚本会自动检查 Python 和依赖
3. 如果提示缺少 .env，会自动创建并打开
4. 填写完 API 密钥后，再次运行脚本

### Mac
1. 打开终端
2. 进入项目目录: `cd ~/seo-content-generator`
3. 运行启动脚本: `bash 快速启动.sh`
4. 按照提示操作

---

## 重要提示

### ⚠️ .env 文件包含敏感信息

**不要**将 `.env` 文件分享给他人！

**如果**需要在公开渠道分享项目，请：
1. 删除 `.env` 文件
2. 只保留 `.env.template`
3. 提醒用户自己创建 `.env` 并填写密钥

### ✅ 检查清单

复制项目前，请确认：
- [ ] Python 版本为 3.11 或 3.12
- [ ] 已保存好所有 API 密钥的备份
- [ ] 知识库文件 (`config/knowledge/`) 完整
- [ ] `.env` 文件已包含所有必需的密钥

---

## 文件大小参考

完整项目大小：约 **2-3 MB**（不含输出文件）

必需文件大小：约 **500 KB**（不含输出和缓存）

---

## 更新项目

如果在新电脑上修改了代码，想要同步回原电脑：

1. 只需复制修改过的 `.py` 文件
2. 或者使用版本控制工具（如 Git）同步
